services:
  - type: web
    name: vcc-landing
    runtime: static
    buildCommand: echo "Static site, no build needed"
    staticPublishPath: .
    envVars: []
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
